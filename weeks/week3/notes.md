# Week 3 Notes

- Day 1:
  - 目标：熟悉 `lldb` 的断点、单步、反汇编、寄存器、栈回溯。
  - 任务：编译并运行 `mini_bomb`，先不看源码，用 `disassemble -n phase_1` 和 `thread step-inst` 追踪第一关。
  - 记录：今天最常用的 3 条命令是什么，为什么。
- Day 2:
  - 目标：练习从循环与数组访问中反推输入。
  - 任务：继续分析 `phase_1` 和 `phase_2`，重点看 `strlen`、`sscanf`、循环边界与递推关系。
  - 记录：哪些指令模式最容易认出来，哪些最容易看错。
- Day 3:
  - 目标：熟悉分支链、`switch`、递归调用。
  - 任务：分析 `phase_3` 与 `phase_4`，画出控制流和递归展开过程。
  - 记录：把某个分支条件翻译回 C 语言含义。
- Day 4:
  - 目标：在不依赖源码的情况下完整解完 mini-bomb。
  - 任务：把解题过程写进 `mini_bomb_solve.md`，并尝试改 1 关输入逻辑后重新编译验证。
  - 记录：哪一关最适合练习栈帧追踪，为什么。
- Day 5:
  - 目标：复盘汇编阅读套路。
  - 任务：总结“看函数入口 -> 看参数 -> 看比较/跳转 -> 看循环/递归”的固定流程。
  - 记录：整理一份自己的 5 条 `lldb` 高频命令和 3 个常见误区。
