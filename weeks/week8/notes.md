# Week 8 Notes

- Day 1:
  竞态出现在哪个共享状态上：
  为什么 `unsafe` 有时又“看起来没问题”：
  修复方法：

- Day 2:
  队列的共享字段：
  `not_empty` / `not_full` 各自保护什么：
  如果忘记加 `while` 会怎样：

- Day 3:
  死锁模式里谁先拿了哪把锁：
  满足了哪些死锁条件：
  为什么这个问题不一定每次都稳定复现：

- Day 4:
  统一加锁顺序后的规则：
  为什么它能打破循环等待：
  还有哪些替代方案：

- Day 5:
  粗锁 vs 细锁：
  我会怎么选线程数：
  本周最值得复用的并发模板：
