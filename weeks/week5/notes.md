# Week 5 Notes

- Day 1:
  跑通的最小链路：
  我确认的系统调用顺序：
  还没解决的问题：

- Day 2:
  新增的 builtin：
  参数解析里最容易出错的边界：
  PATH 查找的验证方式：

- Day 3:
  重定向前的现象：
  重定向后的现象：
  为什么只该影响子进程：

- Day 4:
  shell 自己的进程组策略：
  前台 job 的进程组策略：
  `SIGINT` / `SIGTSTP` 现在转发给谁：

- Day 5:
  作业表字段设计：
  `jobs` / `bg` / `fg` 的状态迁移：
  我用哪个 trace 做了回归：
